sandbox:
  build:
    dockerfile: Dockerfile.sandbox
  image: sandboxeed-codex-example
  volumes:
    - ~/.codex:/home/node/.codex
  domains:
    - .github.com
    - .githubusercontent.com
    - .openai.com
    - .chatgpt.com
