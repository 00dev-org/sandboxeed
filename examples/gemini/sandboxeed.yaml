sandbox:
  build:
    dockerfile: Dockerfile.sandbox
  image: sandboxeed-gemini-example
  volumes:
    - ~/.gemini:/home/node/.gemini
    - ~/.gitconfig:/home/node/.gitconfig:ro
  domains:
    - github.com
    - githubusercontent.com
    - .googleapis.com
    - .google.com
